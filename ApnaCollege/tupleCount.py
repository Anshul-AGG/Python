# count the students scoring A in tuple

score = ("C", "D", "A", "B", "A")

student = score.count("A")
print(student)

# store the above values in list and sort them from A to D

score =["C", "D", "A", "B", "A"]
score.sort()
print(score)

